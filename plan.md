# Plan

Build paper 111 `open_robot_data_coverage_gaps` from the shared pool, compile PDF to Downloads only, and publish the exact-name public repo.

Continuation rule: the v4.1 audit must remain evidence-bound. Keep `STRONG_REVISE` only if the rerun reproduces the mechanism-coverage selection advantage, diagnostic improvement, tail/redundancy/cost non-regression, paired-seed wins, ablation margin, stress-sweep ordering, and artifact-location gates. Do not mark ICLR-main-ready without real public-dataset validation and downstream trained-policy evidence.
